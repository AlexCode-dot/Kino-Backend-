<% layout('layouts/main') %>
<div class="movie__container">
  <article class="movie">
    <h1>
      <%= movie.title %>
    </h1>
    <div class="intro">
      <%- movie.intro %>
    </div>
    <% if (movie.image) { %>
      <img src="<%= movie.image.url %>">
      <% } %>
  </article>
</div>